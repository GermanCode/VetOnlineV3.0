@model IEnumerable<VetOnlineBeta.docente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_creacion_d)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Telefono)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.persona.Nombres)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_creacion_d)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefono)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.persona.Nombres)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDocente }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDocente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDocente })
        </td>
    </tr>
}

</table>
