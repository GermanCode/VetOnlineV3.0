@model IEnumerable<VetOnlineBeta.mascota>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreMasc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_nacimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.persona.Nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.raza1.DescRaza)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipomascota.descTipoMasc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreMasc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_nacimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.persona.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.raza1.DescRaza)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipomascota.descTipoMasc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idMascota }) |
            @Html.ActionLink("Details", "Details", new { id=item.idMascota }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idMascota })
        </td>
    </tr>
}

</table>
